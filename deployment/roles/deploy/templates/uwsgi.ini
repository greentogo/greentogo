[uwsgi]
chdir = {{ project.srcroot }}
# virtualenv
home = {{ project.virtualenv }}
module = config.wsgi

master = true
processes = 4
#socket = {{ project.root }}app.sock
#chmod-socket = 666
socket = 0.0.0.0:9000
http-socket = 0.0.0.0:8000

uid = {{ project.user }}
